# Debt ledger (cross-arc)

Statuses: `open` / `scheduled(slice-N)` / `scheduled(node-N)` / `discharged(link)` /
`deferred(user, date)` / `accepted-risk`. Escalation: an open P2 whose
origin is ≥ 3 completed slices ago must appear in the next review's
Candidate options (see the `choosing-next-slice` skill). Append rows; never
delete — flip status instead.

| id | born | severity | statement | status |
|---|---|---|---|---|
| D-1 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P2 | Latent asymmetric-SDK-drift misdiagnosis in `cross-target-compile.sh`: `wasm_install_precheck` records failure states only, never success — a bundle that installs but lacks one kind's SDK id re-runs the full bounded-retry ladder against the already-installed bundle and reports `sdk_install_failed` where the truth is `sdk_unresolved_after_install`; fix is a third precheck state (e.g. `bundle_installed_ok`) recorded on successful shared install. Latent under the current 6.2.1 pin, which provides both ids | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): `WASM_BUNDLE_STATE` records `bundle_installed_ok` on successful shared install; `wasm_install_precheck` row 5 returns `sdk_unresolved_after_install`; the short-circuit prints `reason=bundle_installed_id_unresolved`. End-to-end drift scenario asserts exactly one install across both kinds |
| D-2 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P2 | Plan-assertion executability: slice 47's plan carried 16 of 29 assertion sites that could not fail and 4 that could not pass — every gate the plan placed on its own correctness was eyeballed. Encode the review's three conventions (never put a check on the left of a pipe; never `echo "…=$?"` after a command whose exit is insensitive to the invariant; a plan must not assert its own HEAD commit nor both mandate inserting a string and assert zero occurrences of it) into plan-writing practice | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): four plan-assertion conventions in `AGENTS.md`, applied to slice 51's own plan (audited in the review: 0 checks left of a pipe, 13 `${PIPESTATUS[0]}`, no unassigned `$SCRATCH`) |
| D-3 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | Retry logfile overwrite in `cross-target-compile.sh`'s bounded-retry ladder: every attempt writes the same `$logfile`, so `print_log_tail` shows only the last attempt's output when diagnosing an install failure. Debt from the slice 45/46 script work; first review row is slice 47's P3 #6 | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): attempt `i` writes `${logfile}.attempt-${i}` via the pure `attempt_logfile`; the ladder prints one labelled tail per attempt on exhaustion and `prepare_wasm_sdk`'s single `print_log_tail` call is gone |
| D-4 | ruleset config (AGENTS.md bypass caveat) | — | Bypass-capable actors can override the `Main` ruleset; required checks bind normal PR flow only | accepted-risk |
| D-5 | [slice 48 spec](specs/2026-07-21-outer-loop-codification-design.md), Decision 8 | P3 | No mechanical enforcement of the review output contract (e.g. a grep test over `docs/superpowers/reviews/`) — deliberate: watch the ritual run manually for at least one arc first | deferred(user, 2026-07-21) |
| D-6 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | Shell-purity exemption set for `cross-target-compile.sh` is unpinned — it exists in prose only; the `flagName` named-and-justified exemption-set pattern in `WorkflowShapeTests.swift` is the model to copy | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): `SELF_TEST_COVERED`/`SELF_TEST_EXEMPT` at top level with a justification per exempt entry, enforced by a two-direction partition + a coverage check under four anti-tautology rules; the detector underneath was widened and pinned in the same slice (review P3 #3) |
| D-7 | [slice 46 review](reviews/2026-07-20-slice-46-post-slice-review.md) | P2 | Harvester provenance gap (carried since slice 42): `harvest-gate-corpus.sh` selects rows by run id alone, with no conclusion/event/fork check, so a fork PR's run could in principle print fabricated `p95_ns=` lines that a later harvest ingests — the only unverified link in the calibration chain under twelve blocking budgets | discharged([slice 54](verification/2026-08-23-calibration-chain-hardening.md)) — `harvest-gate-corpus.sh` now checks each candidate run's `.head_repository.full_name` and fails closed when it cannot be read, on both entry paths (`--runs` included), with no opt-out. A second hole found while reading for it is closed in the same slice: the corpus gained a sixth **verdict** column, and both consumers reject `{budget_exceeded, budget_absolute_exceeded, operation_failures}` at read time, pinned across languages by `testAdmissibleRowsMatchDeriveScript`. Both guards are preventive and their only evidence is the recorded drills — no fork has ever run CI, and no contaminated row exists. **Hardened after the slice-54 validation pass**: the provenance check was pinned only as a pure function, so deleting its CALL SITE from the harvest loop left every automated check green while a fork's run and an unreadable-provenance run both emitted rows. `harvest-gate-corpus.sh --self-test` now drives the whole script through a stubbed `gh` and asserts the rows reaching stdout; both call sites (`admissible_source` and `extract_rows`) redden when removed. Hosted proof on the **merged** tree: push run `32660537137` on `e97791f`, three jobs, 46 `gate=pass`, 408/0 |
| D-8 | [slice 46 review](reviews/2026-07-20-slice-46-post-slice-review.md) | P2 | Bulk-edit absolute backstop (carried since slice 43): `bulk_structural_mutation` remains exempt from the absolute product ceiling (`BenchmarkMode.isFrameHotPath` excludes it); real recorded gap — needs a product-target decision before any ceiling can be derived | discharged([slice 52](verification/2026-08-08-gate-recalibration-and-bulk-ceiling.md), [PR #123](https://github.com/maldrakar/swift-text-engine/pull/123)): the user's product target (one whole 60 FPS frame for a discrete action) unblocked it; a total two-class `AbsoluteCeiling { scrollFrame, discreteAction }` replaces `isFrameHotPath: Bool`, so **no mode is exempt** — `bulk_structural_mutation` is `.discreteAction` at `GateLimits.frameNanoseconds` = 16_666_666 ns, FIXED, and its gate line now prints a number where it printed `exempt`. `testEveryGatedBudgetIsUnderItsClassCeiling` lost its filter and pins all **46** gated budgets (was 41) against their class ceiling; it is the static enforcement point, the runtime `budget_absolute_exceeded` reason is defense-in-depth |
| D-9 | [slice 46 review](reviews/2026-07-20-slice-46-post-slice-review.md) | P2 | p95 thin axis (carried since slice 41): when the windowed 3x-max floor term relaxes, a p95 budget rests on the 8x-median backup term alone — the thin axis to watch at every re-derivation. **Statement amended in place by [slice 52](verification/2026-08-08-gate-recalibration-and-bulk-ceiling.md), not re-scoped** (the D-15 precedent): the *shape-transition half* is **discharged by fact** — slice 52's harvest flushed the window, which now holds **no** pre-slice-45 run (smallest window id `29701547123`, above the `29692848870` boundary) and **no** run contributing more than one `realistic_provider` row (16 such runs before the harvest → 0 after). That check is **window-scoped**: whole-corpus legacy-shape runs went 33 → 38 in the same harvest and that is correct — the corpus is append-only and the harvester still reads the pre-slice-45 line shape on purpose. The *thin-axis half* stays **open**, and now carries the **rule and the ratio rather than a list**: a p95 budget is median-governed exactly when `max / med <= 2.67` (equivalently `8 x med >= 3 x max`), true for **45 of 46** scenarios after this harvest; the remaining max-governed one is the exposed scenario. `derive-gate-budgets.sh` now prints `gov_p95=median\|max` per scenario, so the observation is re-derivable on demand instead of transcribed — and it must be: the single max-governed scenario moved from `line_query\|uniform_1k` to `column_query\|prefixsum_100k` in this one harvest, so a named list would run to nearly every scenario and be stale on arrival — deferred(user, 2026-08-09) — surfaced at the slice-53 selection under the escalation rule (≥ 3 completed slices old; the slice-52 review's escalation check passed over it). Rationale: slice 52 converted the open thin-axis half from a stale named list into a **re-derivable observable** — `derive-gate-budgets.sh` prints `gov_p95=median\|max` per scenario — so the watch is answerable on demand at each re-derivation rather than transcribed into a doc that is wrong by the next harvest. Re-affirm or schedule at the next review. **Re-affirmed deferred(user, 2026-08-23)** at the slice-54 selection: the user scheduled D-7 and left D-9 where it stood. The rationale is unchanged and the watch is answerable on demand (`gov_p95=median\|max` per scenario), but note this is now the *second* consecutive re-affirmation of an item born in slice 46 — the next review should schedule it or state why the observable is a permanent substitute for a fix. |**scheduled(node-6)** — rehomed at the slice-56 brainstorm, 2026-09-04: it is calibration arithmetic and node 6 is the next `harvest → derive` event, where its read-set already lives |
| D-10 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | `AGENTS.md` still cites `docs/superpowers/verification/2026-06-16-swift-ci-required-checks.md` as a live "see" reference while that doc carries the retired `WASM cross-target observation` context name at 8+ sites with no superseded banner — a reader following the pointer for current policy finds the old name in ruleset JSON | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 7): the record now opens with a superseded banner naming the current context (`WASM cross-target compile`), the slice that renamed it, and the fact that every old-name occurrence below — ruleset JSON included — is preserved as the record of what was true on 2026-06-16 and is not current policy. The body is byte-unchanged on purpose: a record is evidence, and rewriting it to match a later fact falsifies it |
| D-11 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | The required-check pin models job `name:` only: job-level `strategy:`/`if:` (which change or suppress GitHub's reported context) are unmodeled and the job set itself is unpinned, so a fourth required job would be silently missed — the repo's own "pins must model what runtime reads" lesson, recurring | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 7): `WorkflowShapeTests.testWorkflowJobSetIsExactlyTheThreePinnedJobs` pins both the workflow's job **keys** and its job **names** against `requiredCheckContexts`, via a new `allJobKeys()` walking the top-level `jobs:` block. Drill (v) appended a fourth job and reddened exactly that test's two assertions while all fourteen other tests in the file stayed green — the gap demonstrated, not asserted. **Scope, stated because the row's statement has two clauses:** the job-set clause is what this discharges. Job-level `strategy:`/`if:` are still unmodelled; no job in `swift-ci.yml` carries either key today (checked at discharge), so a job that *gains* one stays invisible to every pin in that file, while a job that *arrives* no longer is |
| D-12 | [slice 49 review](reviews/2026-07-23-slice-49-post-slice-review.md) | P3 | Interior exact-equal wrap-width boundary untested: `greedyEnd`'s inclusive `columnOffset(c) − startOffset <= wrapWidth` is exercised only at the whole-line edge (the equivalence oracle at `width == total`), never at an interior break where a legal end lands exactly on the width. Operator correct by inspection; a `<`-vs-`<=` mutation would redden the whole-line case but not an interior-only fixture. One-line fold-in for node 2 (Slice 50), which touches the same cursor | discharged([slice 50](reviews/2026-07-25-slice-50-post-slice-review.md), [PR #117](https://github.com/maldrakar/swift-text-engine/pull/117)): `testInteriorExactEqualWidthBoundary` added with recorded `<=`→`<` mutation (`endColumn` 2→1) |
| D-13 | [slice 50 review](reviews/2026-07-25-slice-50-post-slice-review.md) | P3 | Per-axis binary-search default triplication: `binarySearchLogicalLine` (`VisualRowLayoutSource.swift`) is the third copy of the same body — after `binarySearchLineIndex` (`LineMetricsSource.swift`) and the column-axis default — differing only in the accessor (`firstVisualRow`/`offset`) and target type (`Int`/`Double`). Plan-governed (spec Decision 1 ratifies a per-axis default); each copy is correct; a bug in the pattern must be fixed in three places. Consolidation into a shared generic helper is a later cross-axis slice (fold-in candidate) | open |
| D-14 | [slice 51 spec](specs/2026-08-07-cross-target-script-hardening-design.md), Decision 8 | P3 | The coverage/exemption classification is pinned for `cross-target-compile.sh` only; `derive-gate-budgets.sh`, `harvest-gate-corpus.sh`, and `detect-docs-only-pr.sh` now have their self-tests enforced by `swift test` but carry no classification, so a new unexercised function in any of them is silent | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 8): all three scripts now carry the five classification helpers copied verbatim from `cross-target-compile.sh:302-352`, a `SELF_TEST_COVERED`/`SELF_TEST_EXEMPT` partition, and the two-direction partition check — the check block was written once to a file and spliced into each script, so the three copies are identical **by construction** rather than by three hand-pastes. Drill (w) appended `scratch_unclassified_fn` to each and reddened all three (`self_test=fail label=classified_scratch_unclassified_fn`), and the phantom direction (a covered-but-undefined name) reddened too. Two real defects the splice found by being run rather than inspected: bash 3.2 under `set -u` treats a declared-but-**empty** array expansion as `unbound variable`, so the copies use `"${ARR[@]+"${ARR[@]}"}"` where the reference script uses the bare form its own non-empty arrays make safe; and the four non-`assert_*` helpers must appear in their own `SELF_TEST_COVERED`. `lint-plan-assertions.sh` was checked and is out of scope: its dispatcher is a bare call with **no** trailing `exit 0`, so its status propagates through `main "$@"` |
| D-15 | [slice 51 spec](specs/2026-08-07-cross-target-script-hardening-design.md), whole-branch review | P3 | Dispatcher shape asymmetry across the four `.github/scripts` self-tests: slice 51 changed `cross-target-compile.sh`'s dispatcher from `run_self_test; exit 0` to `run_self_test \|\| exit 1`, leaving `derive-gate-budgets.sh:77` and `harvest-gate-corpus.sh:117` on the old shape. **Statement corrected by the [slice 51 review](reviews/2026-08-08-slice-51-post-slice-review.md) (P3 #1):** the originally-recorded trigger — a `return`-based failure being swallowed by the trailing `exit 0` — *cannot fire*. Both sibling scripts run `set -euo pipefail`, so `-e` aborts the shell before `exit 0` is reached (measured: `run_self_test` returning 1 exits **1** in both, and **0** in `cross-target-compile.sh`, the only one of the four without `-e` — which is exactly why it needed the fix). Remaining substance: a cosmetic consistency nit, not latent risk. Fold-in candidate. **Statement amended a second time by [slice 57](verification/2026-09-04-wrap-memory-shape.md) (Task 8), which attempted the fold-in and FALSIFIED it:** the prescribed shape is the unsafe one under `set -e`, so converging on it is a regression in two scripts and inert in the third. The three dispatcher edits were made, measured, and reverted; the row therefore stays **open** with its remedy withdrawn, and the evidence plus the shape a real fix would take are recorded as D-47. Not "fold in next time" — the fold-in as written must not be done | open |
| D-16 | [slice 51 spec](specs/2026-08-07-cross-target-script-hardening-design.md), Decision 8 | P3 | The `--self-test` coverage check strips comments and declaration lines from the extracted body but **not string literals**, so a function name appearing inside a quoted assertion label would count as a reference and let an unexercised covered function pass. Deliberate: a shell-grade string parser costs more than the leak is worth, and the partition check still forces every function to be classified either way. No label in the file equals a function name today. Surfaced from the design into the ledger by the [slice 51 review](reviews/2026-08-08-slice-51-post-slice-review.md) so the accepted residual is visible cross-arc. Trigger to revisit: the first assertion label that equals a function name | accepted-risk |
| D-17 | [slice 52 verification](verification/2026-08-08-gate-recalibration-and-bulk-ceiling.md), Task 5 | P2 | **The `${PIPESTATUS[0]}` idiom that `AGENTS.md`'s own D-2 plan-assertion conventions recommend by name (rule 1 of `AGENTS.md`'s `### Plan-assertion conventions (D-2)`) does not work in this repo's default shell.** Agent command blocks run under **zsh**, which does not populate `PIPESTATUS` at all (it spells the array `pipestatus`, and indexes from 1), so `${PIPESTATUS[0]}` expands to the **empty string**. It does not merely go quiet: `[ "" -eq 0 ]` evaluates **true** in zsh, so the assertion **inverts into a pass** — precisely the un-failable-assertion defect D-2 exists to prevent, recommended by the rule itself. Observed live in slice 52 Task 5 (`echo "build_exit=${PIPESTATUS[0]}"` printed empty; the exit status had to be re-checked with a plain `$?`). Scope: **no committed script is affected** — all four `.github/scripts/*.sh` are `#!/usr/bin/env bash` and none uses `PIPESTATUS`. The blast radius is agent-run plan/verification command blocks — **including the 13 `${PIPESTATUS[0]}` sites the slice-51 review counted as *evidence of D-2 compliance*** (this ledger's D-2 row; [slice 51 review](reviews/2026-08-08-slice-51-post-slice-review.md) row 7), which are themselves un-failable by this finding, so that compliance evidence does not hold. Remediation to record, **not applied here** (choosing the replacement idiom is its own decision — this branch does edit `AGENTS.md`, in three commits, but rule 1's wording was not one of them): recommend a shell-agnostic form instead — `if ! cmd > /dev/null 2>&1; then … fi`, or `set -o pipefail` inside an explicit `bash -c`, or simply not piping | discharged([slice 56](verification/2026-09-04-artifact-shape-enforcement.md)): D-2 rule 1 rewritten in `AGENTS.md` — drops the `${PIPESTATUS[0]}` recommendation by name and gives three shell-agnostic idioms in preference order (do not pipe; redirect then read; wrap an unavoidable pipeline in `bash -c 'set -o pipefail; …'`) — and mechanized as R1 in `lint-plan-assertions.sh`, which flags `PIPESTATUS` inside any non-exempt plan's `bash`/`sh` fence |
| D-18 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | The plan's own AC9 checksum assertion fails on a healthy run: Task 8 Step 3 asserts `[ "$N" -eq 46 ]` over `extract_checksums` applied to a hosted run log, but the literal function yields **54** — the non-gate `memory_shape` (5) and `memory_observation` (3) diagnostic steps also print `mode=`/`scenario=`/`checksum=` fields. The *local* §9 extraction never saw it because that capture loops only the twelve gated modes. Fixed in-place in slice 52's verification §12.5 with a `grep -v` filter, but the plan **template** is unfixed and the next slice copying the AC9 recipe will rediscover it. Fold-in for whichever slice next writes an AC9 step | discharged([slice 55b](verification/2026-09-03-wrap-point-query.md)): the AC13 checksum-extraction step is written with the `grep -v -e 'mode=memory_shape' -e 'mode=memory_observation'` filter, so the hosted count reads 46 and not 54, and the plan contains no `${PIPESTATUS[0]}` (D-17) |
| D-19 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | `frame-hot-path` vocabulary outlives the concept in test prose: five sites in `GateLogicTests.swift`, including the test *name* `testAbsoluteCeilingFiresForFrameHotPathMode` whose sibling reads `…ForBulkModeAtItsOwnCeiling`. Nothing is false — the concept maps 1:1 onto `.scrollFrame` — and slice 52's AC5 scans are identifier-based so they cannot see prose. Renaming restores symmetry. Fold-in for the next slice touching this file | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 6): the test is `testAbsoluteCeilingFiresForScrollFrameMode` and the three surviving comments read `.scrollFrame`; `rg -i 'frame.hot.path' Tests/ Sources/` is empty. `docs/` is deliberately untouched — five historical hits there are evidence records, not sites to fix. A second instance of the same rot was found and fixed in the same task: the comment above `absoluteCeiling` still said the non-gateable modes' class was unpinned, which D-20's own new test had just made false — and that sentence is the one D-20's row cites as where the gap is documented |
| D-20 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | The five non-gateable modes' `AbsoluteCeiling` class is pinned by nothing: `testDiscreteActionClassIsExactlyDocumented` filters on `isGateable`, so `rangeOnly`, `memoryShape`, `memoryObservation`, `wrapCompute`, and (as of slice 53) `wrapRowQuery` classify under a total function but no test covers the choice. Documented at the `absoluteCeiling` switch itself ("do not expect a test to catch a wrong choice here") and inert today, because the absolute tokens are emitted only inside the `includeGate` branch. **The inertness ends the moment a wrap mode becomes gateable** — i.e. at wrap map node 6, where this is the row to read | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 6): `GateLogicTests.testNonGateableModesClassifyAsScrollFrame` derives the non-gateable set from `BenchmarkMode.allCases` (never a transcribed list) and pins each member's class, so a seventh non-gateable mode fails that test until someone makes its class a decision. Drill (t)(i) moved `.wrapPointQuery` into the `.discreteAction` arm: only that test reddened, while both `isGateable`-filtered siblings and all fourteen `GateFloorTests` stayed green — the gap demonstrated rather than asserted. The `absoluteCeiling` comment this row cites ("do not expect a test to catch a wrong choice here") was rewritten in the same task to name the new pin. **What is NOT discharged** is the row's runtime consequence — the class only starts being read when a wrap mode becomes gateable, which is still node 6's business |
| D-21 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | A new **gateable** mode landing in the `.scrollFrame` arm trips no pin. The class-membership pin is bidirectional for `.discreteAction` (moving `bulk_structural_mutation` out, or any other gated mode in, reddens it), but the `.scrollFrame` arm is unpinned. Safe by default — `.scrollFrame` is the *tighter* ceiling and `GateFloorTests` still catches an over-budget mode — but the safety is an accident of which arm is larger, worth naming before wrap modes arrive. **Confirmed live by the [slice 53 review](reviews/2026-08-22-slice-53-post-slice-review.md)** (drill B): flipping `wrapRowQuery` into `isGateable`'s true arm reddens `GateFloorTests.testEveryGateableModeIsRegistered` immediately, while `GateLogicTests` stays 27/27 green — nothing checks the new mode's `AbsoluteCeiling` class. D-20's trigger is reachable by the same drill | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 6): `GateLogicTests.testGateableScrollFrameClassIsExactlyDocumented` pins the `.scrollFrame` half of the gateable set **and** asserts that the two class pins partition it, so a gateable mode can no longer sit in neither and be checked by neither. Drill (t)(ii) moved `.columnQuery` into `.discreteAction` and reddened both assertions of that test plus its `.discreteAction` sibling; `GateFloorTests` stayed green, which is itself the point — moving a mode to the looser ceiling cannot trip a budget-under-ceiling check, so only a class pin can see it |
| D-22 | [slice 53 spec](specs/2026-08-09-wrap-row-query-design.md), Decision 2 | P3 | Uniform-axis inverse hooks are unimplemented: `UniformLineMetrics` (`LineMetricsSource.swift:103`) overrides neither `lineIndex(containingOffset:)` nor `firstLineIndex(withOffsetAtOrAbove:startingAtLine:)`, so every consumer pays an O(log N) binary search where a uniform axis answers by division with a bounded ±1 correction. Two paths, and they differ in cost: making the **public** type native moves four gated modes' hot path (`line_query`, `line_geometry_query`, `point_query`, `point_geometry_query` all construct it for their `uniform_*` scenarios) and therefore drags a budget re-derivation in the same PR per the headroom-ceiling rule; an **internal** `UniformRowAxis` constructed only by `compute(_:layout:)` and `visualRowAt` moves no gated mode and needs none. Would retire the wrap arc's "O(log totalRows), not literally width-independent" correction on the row axis. Natural home for **D-13** (it edits `LineMetricsSource.swift`, where two of the three binary-search copies live). Watch-item for whichever path is taken: node 2's ∞ oracle compares `compute(_:layout:)` against `UniformLineMetrics`, so a division-based wrap axis would make that oracle compare two different search implementations — stronger when they agree, and the first place a ±1 rounding disagreement surfaces | open |
| D-23 | [slice 53 review](reviews/2026-08-22-slice-53-post-slice-review.md) | P2 | Both wrap benchmark modes (`--wrap-compute`, `--wrap-row-query`) time **one** operation per `clock.measure`, where every gated mode amortises over `operationsPerSample = 256` and divides (`LineQueryBenchmark.swift:73-89`). Measured locally in release, the wrap output is quantised to the host clock tick (≈41.7 ns: 83/167/250/292 ns are 2/4/6/7 ticks) and `uniform_1k` collapses to `p95 == p99`, while the gated siblings this query belongs beside measure 17-94 ns — i.e. below one tick. The numbers resolve the clock, not the operation. Node 6 promotes wrap modes via `harvest -> derive`, which never re-measures, so promoting on this shape anchors a budget to clock overhead — the slice-27/31/33/35/37 "gate that cannot fail" failure, with a deadline: it must be repaired **before** the first wrap harvest, not after. One repair covers both modes | discharged([slice 54](verification/2026-08-23-calibration-chain-hardening.md)) — both wrap modes now measure through `amortisedSamples` (one clock read per `operationsPerSample` operations, divided by the separately-pinned `amortise`), so their numbers resolve the operation rather than the host tick. `--wrap-compute`'s `reindex_ns` stays a single unamortised measurement by decision, printed as `reindex_operations_per_sample=1`; its construction is now bound and reused, removing a second O(N) pass and making the timed work observably live. Repaired **before** node 6's first harvest, as the deadline required |
| D-24 | [slice 53 review](reviews/2026-08-22-slice-53-post-slice-review.md) | P2 | `VisualRowLayoutSource.logicalLine(containingVisualRow:)` is documented as "provider-overridable, binary-search default" in `AGENTS.md`, in the protocol, and in `visualRowAt`'s doc comment — and that contract is pinned by **nothing**. Drill C in the review replaced the dispatch in `WrapPositionQuery.swift` with a direct `binarySearchLogicalLine(...)` call and the full suite stayed **397/0**. `grep -rn "func logicalLine(containingVisualRow"` returns exactly two hits: the requirement and the default. Contrast the line axis, where `BalancedTreeLineMetrics` overrides the native hooks and `ComputeNativePrefixSearchTests` / `LineAtQueryCountTests` / `LineGeometryAtQueryCountTests` exercise the override path. Inert today (no conformer overrides it); the failure lands when one does and its O(log N) descent becomes dead code with no test noticing. Fix is one test-only conformer that overrides the hook and asserts it was called — a fold-in, natural home node 4 | discharged([slice 55a](verification/2026-08-28-wrap-point-query-trap-repairs.md)): `VisualRowDispatchTests` on `OverridingLogicalLineLayout` (hook overridden with the correct answer, calls logged) pins one dispatch per `visualRowAt` (in range and at both clamp edges) and one per `DocumentVisualRowCursor` for the buffer start; bypassing either site reddens (`[] != [5]`). Landed **after** the five producer guards of the same slice, because the conformer is what makes the malformed-override category live |
| D-25 | [slice 53 review](reviews/2026-08-22-slice-53-post-slice-review.md) | P3 | `WrapRowQueryCountTests.testProbeCountDoesNotGrowLinearlyWithTheDocument` asserts `totalCalls < lineCount / 10` = 102 while its sibling asserts `<= 14` on the same fixture. The parked framing ("no implementation can pass one and fail the other") is slightly overstated — the two probe different `y`, so they are not formally nested — but no independent failure mode is on record: under drill 5 Form A the 102 bound fired only alongside the sibling (1004 vs 102, 704 vs 14). Redundant rather than un-failable; tighten it to claim something the sibling does not, or fold it in | discharged([slice 55b](verification/2026-09-03-wrap-point-query.md)): TIGHTENED and RETARGETED, not removed. `testProbeCountDoesNotGrowLinearlyWithTheDocument` becomes `testTheInRangeWorstCaseTargetHasNoSlackAgainstTheBound`, held to `<= ceilLog2(lineCount) + 4` instead of `< lineCount / 10`. Tightening alone would NOT have discharged it: over 1 024 lines the default `logicalLine` search costs 10 probes at almost every target, so row 700 (the sibling) and row 1 000 (the old target) both measure 13 — same fixture, same branch, same count, same bound, which is this row's own defect restated. Only rows 1 022 and 1 023 reach the 11-probe worst case, so the target moves to row 1 022, where the count is exactly 14 and the bound carries zero slack. That is the claim the sibling (one probe of slack) does not make, and it is reached on the IN-RANGE branch, unlike `testClampedQueriesStillSearchTheLayoutAxis`'s own 14 |
| D-26 | [slice 54](verification/2026-08-23-calibration-chain-hardening.md) | P3 | Three residuals of the read-time reject set, all accepted rather than fixed. **(a) Two languages.** `testAdmissibleRowsMatchDeriveScript` covers **agreement, not correctness**: if both sides gain the same wrong entry, nothing notices. Same residual the two window pins carry, accepted on the same terms. **(b) Two awk programs — and the same shape once existed in Swift, now closed.** The two Swift readers extracted the verdict column independently and had drifted (`>= 6` in the pinned seam `admissibleCorpusRows`, `== 6` in the production reader `corpusExtremes`); they now share `corpusVerdict`, pinned by `testTheTwoCorpusReadersAdmitTheSameRows` (drill: one reader stops reading the column → 3 red, including the cross-language pin). What remains is the shell half.  On the shell side the rule is written *twice* — in `admissible_rows()` (the `--admissible-rows` seam the cross-language pin drives) and in the derivation's main awk, which never calls the seam — sharing only the `REJECTED_VERDICTS` constant. Both halves are now guarded (the pin covers the seam; `derive-gate-budgets.sh --self-test` drives the **full derivation** over a fixture and fails if a `budget_exceeded` row moves the derived budget — a case whose red was observed, `budget_p95` 200 → 3000000, by deleting the production filter line). The residual is that an edit must land in both programs; a collapse of the two onto one implementation was deliberately deferred as too invasive for the branch that carried AC13 byte-identity. **(c) The reject set is three string literals, not a total partition.** A *new* `GateFailureReason` case meaning "the measurement was slow" would be **admitted by default** — the opposite of `BenchmarkMode.isGateable`, whose exhaustive switch this repo deliberately uses so a new mode cannot become gateable silently. The fix if it ever bites: make `GateFailureReason` `CaseIterable` and assert the reject set plus the admit set partition it. Not taken in slice 54 — `BenchmarkModels.swift` was designated untouched (no gated mode's numbers may move) and the risk is prospective, with no such case proposed. Also related: `operation_failures` is rejected on reasoning rather than measurement — no such row exists to inspect — and a scenario whose every windowed row was rejected would fail `GateFloorTests` with "no hosted evidence", a message pointing at the corpus rather than at the rejections that emptied it; the derivation's per-reason `dropped=` counts now print **ahead of** its `error=no_corpus_rows` exit, so that case is explained on the derive side  **(d) The derivation's `dropped=` counts span every mode even when one is requested** (added by the [slice 54 review](reviews/2026-08-23-slice-54-post-slice-review.md), P3 #4): the verdict filter runs before the mode filter, so a single-mode re-derivation reports rows dropped for modes the operator did not ask about. Cosmetic — the counts are diagnostics, not a parsed format, and are correct for what they claim — but it makes a targeted re-derivation look noisier than it is. Fold-in. | accepted-risk |
| D-27 | [slice 54 review](reviews/2026-08-23-slice-54-post-slice-review.md) | P2 | **Ten of twelve blocking gate steps have unpinned shape.** `WorkflowShapeTests.pinnedGateSteps` pins exactly two (`--point-geometry-query`, `--realistic-provider`) — payload equality, not `continue-on-error`, docs-only guard, ordering anchors. The workflow carries twelve `--gate` invocations. Drill H (recorded in the review, run against `c3603c6`): appending `\|\| true` to the `--line-query --gate` step leaves `swift test` at **408/0** — the gate is disarmed and nothing notices. `\|\| true` and `continue-on-error` are the exact pair `AGENTS.md` names as the Slice 16 dead-step trap. This is the **fourth** instance of one defect class in this repo, and the third found inside slice 54 alone: a guard whose decision is correct and whose **invocation** is unguarded (drill 8 = the derivation's awk filter; drills 9-10 = the harvester's two call sites). Fix already scoped by `pinnedGateSteps`' own comment: a `flagName` property on `BenchmarkMode`, a named-and-justified exemption set (the `.pipeline` default has no flag at all), and a test pinning the two together. The cost read is correct; the new information is that it now buys ten blocking budgets, not a tidier test. Schedule by slice 56 rather than aging it | discharged([slice 56](verification/2026-09-04-artifact-shape-enforcement.md)): `pinnedGateSteps` now holds one row per gateable mode (twelve), pinned to `BenchmarkMode.isGateable` as a bijection in both directions and identified by exact payload equality against a command derived from `BenchmarkMode.flagName`. Appending `\|\| true` to any of the ten previously-unpinned gate steps now reddens on exact-payload-equality — `steps(for:in:)`'s own `XCTAssertFalse(matches.isEmpty, …)`, which underlies both `testExactlyOneStepRunsEachPinnedGate` and `testEachPinnedGateRunsExactlyTheExpectedCommand`, since no step's payload equals the expected command any more. The whole-file `--gate` census (`testEveryGateInvocationInTheWorkflowIsPinned`) closes a different hole: a thirteenth, wholly UNPINNED gate step anywhere in the workflow, which a per-step pin cannot see at all |
| D-28 | [slice 54 review](reviews/2026-08-23-slice-54-post-slice-review.md) | P3 | `amortisedSamples` documents itself as "the measurement shape every gated mode uses (`LineQueryBenchmark.swift:73-89`)". The claim is **true today** — verified line by line at review time: `LineQueryBenchmark.swift:71-89` is one clock read per iteration, `operationsPerSample` operations inside it, and the division at line 89, which the `:89` truncation citation names exactly. But the twelve gated modes deliberately keep hand-rolled loops (slice 54 spec Non-Goal 1: any change to them shifts their numbers and drags a budget re-derivation), so the helper and the loops it mirrors are two implementations of one shape with nothing holding them together. The drift lands at **node 6**, where wrap budgets are derived on the unstated assumption that a wrap number and a `line_query` number mean the same thing. Fix: a test driving the helper and one representative gated loop over the same synthetic body, or a comment that claims "modelled on" instead of equivalence | open |
| D-29 | [slice 54 review](reviews/2026-08-23-slice-54-post-slice-review.md) | P3 | **AC5's "cannot get a test" is overstated.** Slice 54's spec, plan and verification record all state that Decision 4 (the `--wrap-compute` drain body performs no `compute`) is reviewable but not machine-checkable, because `amortisedSamples`' structural test pins the body's call count and a body computing a range inside itself would satisfy it unchanged. That is correct **about `amortisedSamples`' test** and does not generalize: the property is about `BenchmarkWrapLayout`, not the helper. A counting wrapper around the layout — the shape D-24's proposed fix uses, and that `WrapRowQueryCountTests` already uses for probe counts — can assert `compute(_:layout:)` is invoked **zero** times across the drain loop. This is the one guarantee in slice 54's falsifiability audit with no recorded red, so under the choosing-next-slice rule it carries a MANDATORY candidate option; the review recommends folding it into node 4, which touches the same benchmark surface | discharged([slice 55a](verification/2026-08-28-wrap-point-query-trap-repairs.md)): the drain body is `drainVisualRows(_:layout:)`, and `WrapComputeDrainTests` drives it through a counting `BenchmarkWrapLayout` asserting zero `firstVisualRow(ofLine: lineCount)` probes — the probe every `compute(_:layout:)` makes (a witness call proves it) and the drain path never can; a `compute` placed inside the body reddens it (`1 != 0`) |
| D-30 | [slice 54 review](reviews/2026-08-23-slice-54-post-slice-review.md) | P3 | **`reindex_ns` is structurally unharvestable.** `--wrap-compute` prints `reindex_ns=` with no `p95_ns`/`p99_ns` companions, and the harvester requires those **exact** keys — so the width-change cost cannot become a gate through the `harvest -> derive` recipe at all. Not a defect in slice 54 (the unamortised exemption is a ratified decision and the right one), but it is the quantity **criterion 1** is about and the one number in this mode a budget would be interesting for. Node 6 must either accept that `--wrap-compute` gates only `compute_`/`drain_`, leaving the reindex observational permanently, or design a second mechanism for one-shot setup costs. Recorded now rather than decided at node 6 under time pressure | open |
| D-31 | [slice 55a](verification/2026-08-28-wrap-point-query-trap-repairs.md) | P3 | **`--wrap-compute`'s local columns cannot resolve an effect smaller than the host's own state variance.** Slice 55a took two measurements of the *same* commit (`0235e73`, columns C3 and C3b, zero code change between them) and every one of the fifteen timing ratios moved, spanning 0.352-0.943 — up to **2.84x** on one token. A third measurement at `d977248` (post-branch validation) read `width_inf reindex_ns` = 5 927 042 against column C5's 11 923 958, another 2x on an unchanged tree. The consequence is recorded rather than repaired: Decision 12's predicted `reindex_ns` cross-width ordering (`inf` < `40` < `10`) came out **broken** against the cleaner baseline (0.846 < 0.650 < 0.980) and `compute_*` — a path no commit in the slice touches — carried findings in *both* directions depending on which baseline was the denominator. The cost claim survives on `WrapPackingCountTests`, a probe-count pin, which is the stronger evidence anyway; what has no guard is the *inference from the mode's timings*. Node 6 is where this bites: it promotes wrap modes through `harvest -> derive`, which never re-measures, and a budget derived from evidence this noisy repeats the slice-27/31/33/35/37 shape one level up. Two candidate repairs, neither scoped here: measure each column N times and record the spread as the unit of comparison, or promote on hosted samples only and stop reading local columns as evidence about anything below ~3x. Distinct from D-23 (clock quantisation, discharged in slice 54 by amortisation) and from D-28 (helper-vs-loop shape): this is host state, and amortising a sample does not touch it | open |
| D-32 | [slice 55a](verification/2026-08-28-wrap-point-query-trap-repairs.md) | P3 | **The shared within-line walk is pinned by its rule, not by its call site.** `advanceVisualRows(_:by:)` exists so `DocumentVisualRowCursor.init` and node 4's query agree on "row *k* of line *L*" **by construction** (spec Decision 4), and `VisualRowWalkHelperTests` pins the helper's own five-case rule — but nothing reddens if the cursor is reverted to the inline `for _ in 0..<rowInStartLine { _ = inner?.next() }` it replaced. With guard 4 live the inline form is behaviourally identical, so all 425 tests stay green and the by-construction property is silently gone. This is the **fifth** instance of the class the ledger already carries three times — a guard whose decision is correct and whose *invocation* is unguarded (D-24 the row-axis hook, D-27 the ten unpinned gate steps, D-26(b) the two awk programs). No cheap repair exists at the 55a boundary: the two call sites are behaviourally indistinguishable until the second one exists. The natural discharge is **55b**, where the round-trip test holds the streamed row and the queried row equal on a two-kind fixture — if that test is driven through the shared helper on both sides, an inline revert on either breaks it. Record the check at 55b's review rather than adding apparatus here | discharged([slice 55b](verification/2026-09-03-wrap-point-query.md)): no new apparatus, as this row prescribes. `visualPointAt` is the SECOND call site of `advanceVisualRows(_:by:)`, and `WrapPointQueryRoundTripTests` drives both sides through it — the query on one, `DocumentVisualRowCursor.init` (through `visualRowGeometry`) on the other — over a fixture carrying both of `greedyEnd`'s branches, so "row k of line L" agrees by construction rather than by two tests agreeing. The discharge claims OBSERVABILITY, not enforcement, and the record carries its evidence (Task 7 Step 3): reverting the cursor to its inline walk leaves the round trip and all 425 tests green, because guard 4 makes the two forms behaviourally identical — exactly as this row predicted. Enforcement would need a call-site pin rather than a behavioural test, which is a fresh decision and not this slice's |
| D-33 | [slice 55a review](reviews/2026-09-03-slice-55a-post-slice-review.md) | P2 | **`--wrap-compute`'s `checksum=` is the one witness Decisions 12 and 13 rest on, and nothing pins that it can move.** Both decisions are result-preserving by construction, so neither adds a result assertion; what covers them is that a wrong version reddens the existing suite *plus* a `checksum=` token folded over 100 000 lines at three widths — "a result-preservation check on a fixture no unit test reaches" (spec Decision 12). `WrapBenchmarkLineShapeTests` pins only that the token is **printed** with the value `formatWrapComputeLine` was handed (`987654`); it says nothing about the fold. The value is `computeMeasured.checksum &+ drainMeasured.checksum`, so zeroing the drain half — the half that folds every drained row's `endColumn`, i.e. the half that witnesses **packing** — still prints a stable number, byte-identical across every column of the record, and every claim built on that byte-identity silently becomes vacuous. Both sibling checksums in the same test target already close exactly this hole: `WrapRowQueryChecksumTests` asserts each folded index affects the value, and `PointGeometryChecksumTests` exists because AGENTS.md records that "a zeroed multiplier or a reversion to an additive index-only fold cannot pass silently". The wrap-compute token is the odd one out, and it is the one carrying the most weight. Fix is one sibling file (~30 lines, no shipped-code change): drive `runWrapComputeBenchmarks`' fold over two layouts differing in one row's `endColumn` and assert the checksums differ. **MANDATORY candidate option** under the falsifiability rule — a standing guarantee with no recorded red. Natural home 55b, which already adds two benchmark-target test files (spec Decision 10) | discharged([slice 55b](verification/2026-09-03-wrap-point-query.md), spec Decision 15): `WrapComputeChecksumTests` pins both halves — the fold reads both operands (drill (n): returning the compute half alone reddens it while the line-shape pin stays green) and `drainVisualRows` folds every row's `endColumn` (drill (o): folding only the first row reddens it while D-29's own test stays green). The fold's weights stay 1 and 1 on purpose, so the printed value remains byte-comparable with 55a's final column; a weighted fold would buy swap-detection and cost that comparison |
| D-34 | [slice 55a review](reviews/2026-09-03-slice-55a-post-slice-review.md) | P2 | **D-2's plan-assertion conventions are documentation, not enforcement, and slice 55a is the measurement.** D-2 was discharged in slice 51 by writing four rules into `AGENTS.md` ("Plan-assertion conventions (D-2)"). Slice 55a's plan was written *after* that discharge, was smoke-tested, carried an explicit placeholder-and-assertion audit in its own preamble ("every assertion carries its expected output and an `if`/`else` printing both branches; no `${PIPESTATUS[0]}`") — and still shipped **four** defective checks, all found at execution time and all recorded: (1) Task 3 Step 6's `HITS` regex could not pass, because one alternative matched a line identical in the plan's own before/after blocks; (2) Task 6 Step 3's `REVERTED TOO FAR` check is inverted under the commit-first ordering the plan itself permits, so it always fires; (3) the WASM `blocking=true` count is not job-scoped and reads the iOS job's four lines too, printing 8 where the plan expects 4; (4) the plan-supplied `predict.py` indents its `compute_*`/`checksum` checks inside a loop that never rebinds the scenario variable, so nine reported lines are `width_10` evaluated three times and the `width_inf` and `width_40` verdicts the record needed were never computed by it. Three of the four are the **same class** D-2 names — an assertion whose exit semantics do not track the invariant — and the fourth extends the class from shell to plan-supplied analysis code, which D-2's rules do not mention at all. Two readings, and the ledger takes the second: either four defects per plan is the irreducible rate for hand-written checks (in which case say so and stop counting them as slice findings), or the conventions need a mechanical pass — a plan-authoring checklist step that *executes* each assertion against a known-bad tree and requires a red. Related and unresolved: **D-17**, the `${PIPESTATUS[0]}` idiom `AGENTS.md` rule 1 still recommends by name, which inverts to a pass under this repo's zsh. **Shape half discharged by slice 56** (`lint-plan-assertions.sh` R1–R3, run from `swift test` and from an unguarded CI step). The **semantic half stays open**: none of the four defects this row measured is shape-detectable — a regex that could not match, a check inverted under an ordering the plan permits, a count not scoped to its job, and a Python loop that never rebinds its variable. What the linter answers is D-17 and D-2 rule 4, and slice 56's spec §2.1 says so rather than claiming the row | open |
| D-35 | [slice 55b review](reviews/2026-09-03-slice-55b-post-slice-review.md) | P2 | **The spec Contract's drill list is a closed set, while AC14 says "every standing guarantee this slice adds carries a recorded red" — and when they disagree the closed set wins in practice.** Slice 55b measured it: four guarantees shipped without a drill and were caught by two *later* passes — (p) Decision 14's `>= total` answer (the fixture could not separate the right index from a plausible wrong one) and (q) step 7's `!rebased.isFinite` guard (no test at all), both found by the whole-branch review; (r) the layout-axis bound's target (13 probes against a bound of 14 — D-25's own shape, on the file of the very slice that discharged D-25) and (s) the overflow fixture's one-cell row, both found by a pre-merge validation pass. Once the list was quoted *as authority* for not drilling: the record's §9 item 5 discharges AC9's 55b half "by assertion, not by drill" because "Contract 55b's drill list … is the twelve named there". **Mechanism, not lapse:** TDD writes a test red-first when the test *specifies* behaviour; a cost pin, a probe-count bound or a fixture that must separate two indices **measures** the implementation instead, so it is written green from birth and nothing forces the question "can this pin fail?" at the moment it is written. Fix: make the per-task **guarantee inventory** the authority and the spec's list a *lower bound* — each plan task enumerates the standing guarantees it adds and carries a drill step for each. Same family as D-34 (D-2's conventions are documentation, not enforcement); natural home **slice 56**, folded in beside it. **Structural half discharged by slice 56** (R4: every task names its guarantees; every named guarantee needs a step mentioning a drill). **R4 buys a prompt, not a proof** — a task satisfies it with an empty inventory, or with a drill step that names a drill it does not perform. The question is now asked at authoring time, which is what this row measured as missing; whether the answer is honest stays a reader's job | open |
| D-36 | [slice 55b review](reviews/2026-09-03-slice-55b-post-slice-review.md) | P3 | **AC12 asks for a checksum "folding every returned field"; `wrapPointQueryChecksum` folds every *non-duplicated* field.** `rowSpan.logicalLine` and `rowSpan.rowInLine` are deliberately omitted — they duplicate `row.logicalLine` / `row.rowInLine`, which are folded, and agree with them by construction (one cursor walk produces both); the duplication is pinned in the core suite by `testRowSpanAndRowAgreeOnTheirDuplicatedFields`, so nothing is uncovered. Recorded because the narrowing departs from the criterion's literal wording, and because folding them **moves the printed value**, which the record's §6 byte-identity comparison against the shipped run leans on. Fix, when the value is being re-baselined anyway: fold the two at **node 6**, which un-prefixes this mode's latency keys and re-prints the line regardless. Not before — a checksum churned for tidiness costs a comparison and buys nothing | open |
| D-37 | [slice 55b review](reviews/2026-09-03-slice-55b-post-slice-review.md) | P3 | **A verification record cannot carry facts about its own branch, and the convention that solves it is unwritten.** Second instance: 55a's review raised it as its P3 #4 ("a record cannot name its own hosted run, and the repo has no convention for it") and it was never laddered into this ledger, so it recurred one slice later — 55b's record stated its commit count wrong twice ("fifteen", then "seventeen", against an actual 20) and called a hosted run "the current HEAD", false two commits later because §7's own fill-in moved the head. Both are fixed in-slice by devices that now have three instances and zero written statement: the post-merge proof lands on a **separate `slice-N-hosted-proof` branch** (slices 54, 55a, 55b), and self-referential facts are phrased **re-checkably** (a per-head run table; "nineteen by SHA plus this one" instead of a bare count). Fix: three lines in `AGENTS.md`'s workflow section, beside the existing "anchor proof in the post-merge run" rule | discharged([slice 56](verification/2026-09-04-artifact-shape-enforcement.md)): three lines appended to `AGENTS.md`'s `Conventions that matter` list, beside the existing anchor-in-the-post-merge-run bullet — the separate `slice-N-hosted-proof` branch practice and the re-checkable self-referential-fact phrasing, both named rather than left as three unwritten repeated fixes |
| D-38 | [slice 55b review](reviews/2026-09-03-slice-55b-post-slice-review.md) | P3 | **One derived quantity in `WrapPointQueryCountTests` is hand-computed rather than derived from the test's own variables.** The growth bound uses `let mustScan = 55 - 15`, transcribed from the fixture's advances instead of computed from `near`/`far`; correct today, a transcription risk the moment the fixture changes. Worth carrying rather than dismissing because the *sibling* defect in the same file — a fixture guard written as a comparison of two constants, which could not fail — was caught and fixed during Task 5, i.e. the file has already produced one instance of this class. Fix: derive it from the fixture (cells-per-row x the row distance), or assert the literal against a derivation | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 6): `mustScan` is now `(far - near) * cellsPerRow`, with `rowCount`/`cellCount` read from the fixture itself and a divisibility guard (`cellCount % rowCount == 0`) so the quotient is not silently truncated. Certified the hard way: the planned drill could **not** separate the derived form from the transcribed one, because reshaping cell count at a fixed width leaves cells-per-row — and therefore both values — at exactly the same number, and a second attempt failed too because the within-line walk costs ~6 probes per row rather than one per cell, so the observed growth cleared both bounds. What separates them is shrinking the *sample gap*: at `far = 6` growth is 25, which the derived bound of 4 passes and the transcribed 40 reddens |
| D-39 | [slice 55b review](reviews/2026-09-03-slice-55b-post-slice-review.md) | P3 | **The ledger's own table structure is unenforced, and two rows were broken — including the one row whose status matters most.** GitHub-flavored Markdown does **not** protect `\|` inside a code span in a table: a literal pipe splits the cell wherever it appears. D-9 carried one (`gov_p95=median\|max`) and D-27 four (two `\|\| true` spans), so the two rows rendered with 7 and 10 cells instead of 6 — and D-9's **status** column therefore read "max` per scenario)…" instead of `scheduled(slice-56)`. That is the column the escalation rule reads, on the row that has been surfacing for nine slices. Found by a script written to re-derive this review's open counts, not by reading. Both rows are repaired in the same commit as this row. What is missing is the *guard*: nothing checks the invariant, which is a one-liner — every `\| D-` line must contain exactly six unescaped pipes. Natural home: beside D-27's gate-step shape pin in slice 56, since both are "an artifact whose shape nothing verifies" | discharged([slice 56](verification/2026-09-04-artifact-shape-enforcement.md)): `DebtLedgerShapeTests` pins exactly six unescaped pipes per table-body row (five columns), the table's header/separator/id-row extent, unique-and-contiguous ids from `D-1`, and the status-cell vocabulary — a ratchet held over a now-clean artifact, not a one-time repair |
| D-40 | [slice 56 spec](specs/2026-09-04-artifact-shape-enforcement-design.md), D56-7 | P3 | Three assertion classes stay unmechanized by `lint-plan-assertions.sh`: D-2 rule 1 (a check on the left of a pipe) and rule 3 (a plan asserting its own HEAD) need semantics rather than shape, and plan-supplied **analysis code** — slice 55a's `predict.py`, whose checks sat inside a loop that never rebound its scenario variable — is a class D-2's four shell-shaped rules do not name at all. Named at the slice-56 spec (D56-7) rather than left as an unmentioned gap | open |
| D-41 | [slice 56 spec](specs/2026-09-04-artifact-shape-enforcement-design.md), D56-1a | P3 | `BenchmarkMode.flagName` is a **pinned** third copy of every flag spelling, not a deleted one (`BenchmarkOptions.parse`'s case labels are the first, `swift-ci.yml`'s payloads the second). Having `parse` derive its cases from `flagName` would delete the copy instead of pinning it and is strictly better; it rewrites every per-flag "cannot be combined with another mode" message and its tests, which slice 56 declined as option-parsing surgery inside a slice whose fingerprint is "nothing measured moves" | open |
| D-42 | [slice 56](verification/2026-09-04-artifact-shape-enforcement.md) | P3 | **Ten residuals measured during this slice's own implementation (four at ship time, two in the final fix wave, four in the post-record validation pass), laddered explicitly rather than left unmentioned** (the same rule the spec itself names for exactly this situation). (a) **The exemption ratchet's cutoff check is a filename compare, not a date parse.** For any exempt plan that lints CLEAN on its own, a swap is caught by the cutoff check ALONE — the script's own exit status stays green — so a plan deliberately named with a shape-valid pre-cutoff date, swapped in for one of those, would pass all four ratchet checks. The date-SHAPE assertion added in this same slice (`isDateShaped`) narrows this — it rejects an undated or malformed name — but does not close it. How many exempt plans are in that clean set is a property of the current rule set and is re-derived, never transcribed: the command is in `PlanLintTests.swift`'s header, which carries this row's own cautionary tale (its first draft quoted a 35/21 split that the same branch falsified two commits later, when R4's heading widening moved every exempt plan into the dirty set). (b) **`lint-plan-assertions.sh` R3 does not recognize `declare -r VAR=`, `readonly VAR=` or `VAR+=` as assignments**, so those forms produce false positives. Not fixed because the design document enumerates the recognizer list as closed. (c) **R2 misses `echo "…=$?"` when a `;` appears inside a quoted `sed`/`awk` program on the same line**, because the same-line split picks a fragment. A MISS, not a false positive — the acceptable direction — and closing it needs a change to the split logic rather than to the predecessor test. (d) **Gate-block CONTIGUITY is pinned by neither the total order nor the two boundary anchors**: a non-gate step may be interleaved between two gate steps and every `WorkflowShapeTests` check stays green. The old per-row anchor form did not pin it either — its own retired comment claimed the block stayed "contiguous," a claim the test beneath it never actually enforced — so this is not a regression, but the word implied coverage that never existed. (e) **R1–R3 are scoped to `bash` and `sh` fences only.** A plan using a `shell`, `zsh` or `console` fence gets R1–R3 as a silent no-op — the same shape as the R4 heading defect one level down. Not widened here because the spec's AC7 states the scope explicitly and a fence-language census over all 57 plans found none of those spellings in use (bash 1391, swift 443, text 320, yaml 68, markdown 62, diff 16, json 10, sh 8, awk 5, ruby 4). (f) **The spec's §4B R3 allow-list is narrower than what shipped.** The spec enumerates `$HOME`, `$PWD`, `$TMPDIR`, `$PATH`, `$USER`, `$GITHUB_*`, `$RUNNER_*` plus awk built-ins; the script also allows `SHELL`, `IFS`, `OLDPWD`, `RANDOM`, `SECONDS`, `PIPESTATUS` and the `BASH_` prefix. The widening is benign and necessary — `PIPESTATUS` in particular must be allow-listed so R3 does not double-report what R1 reports — but the spec now describes a narrower rule than the tree. Recorded rather than resolved by editing the spec, which is the slice's binding authority and was written before execution. (g) **`lint-plan-assertions.sh` has no guard against a non-portable awk construct.** The heading pattern shipped once as the interval expression `#{2,3}`, which the CI container's awk ignores; R4 was inert there — both heading forms, not just the two-hash one — while the CI step still printed `lint=pass`. It reddened only because `--self-test` runs from `swift test`. Fixed to `###?`, and the file now states the rule (POSIX-portable ERE only), but nothing MECHANICALLY stops the next such construct: authoring runs on macOS awk, enforcement runs on Linux awk, and the two disagree silently rather than erroring. **Statement amended in place by the slice-56 review, not re-scoped** (the D-15/D-9 precedent): as first written this clause claimed no mechanical guard at all, which understates what the incident demonstrated — every rule carries at least one `bad-*` fixture whose self-test asserts exit 1 AND the exact rule name, so a rule that goes inert on any platform reddens `swift test` on that platform, and that is the guard that fired. What is missing is PREVENTION, not detection: the failure surfaces one hosted run later than it could, and (see D-43) not at all on a docs-only PR. A residual that overstates a gap is the same class of unfalsifiable claim as one that understates it. (h) **An arithmetic left-shift by an upper-case name — `$((1 << BITS))` — is read as a heredoc opener with tag `BITS`.** The scanner then skips to end-of-file and reports `violation=scanner` with a wrong diagnosis, leaving the rest of the file unchecked. It fails LOUDLY (exit 1), which is the acceptable direction, and `<<<` here-strings are already handled; this is the remaining phantom. (i) **The fence-language census that justified the `bash`/`sh` scope omits UNLABELLED fences**, of which the plan corpus carries roughly 205 — more than yaml, markdown, diff, json, sh, awk and ruby combined. Measured impact today is small: 10 of them open with a shell-looking command and all 10 are documentation snippets, not assertion blocks. (j) **R1 fires on a shell COMMENT that merely mentions `PIPESTATUS`** inside a `bash` fence — the same class of false positive the fence scope was introduced to avoid, one level in. The workaround is to write the prohibition in prose, which every compliant plan already does | open |
| D-43 | [slice 56 review](reviews/2026-09-04-slice-56-post-slice-review.md) | P2 | **A docs-only PR runs the plan linter but never checks that the linter's rules are alive.** D56-9 deliberately left the `Lint plan assertions` step OUTSIDE the docs-only guard, because a plan is `docs/**` and a plan-carrying PR would otherwise skip the linter entirely. But the guarantee that the rules still WORK — `--self-test`, which drives every rule against a known-bad fixture and requires a red — lives in `ScriptSelfTestTests`, i.e. inside `swift test`, whose step IS guarded (`if: steps.change-scope.outputs.docs_only_pr != 'true'`). So on exactly the PRs the unguarded step exists for, the only thing that runs is a linter that can print `lint=pass files=1 violations=0` with one or more of its four rules checking nothing. Not hypothetical: hosted run `33882695798` is the recorded instance of that output with R4 inert, caught only because that PR was not docs-only. Remedy is cheap and belongs with the first slice that touches CI: have the unguarded step run `--self-test` first and then the lint (~1 s, no Swift build); `WorkflowShapeTests.testPlanLintStepIsBlockingAndUnguarded` pins that step by exact payload, so it is one line of YAML plus one expected-payload literal | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 5): the unguarded `Lint plan assertions` step now runs `./.github/scripts/lint-plan-assertions.sh --self-test && ./.github/scripts/lint-plan-assertions.sh`, so on a docs-only PR — the case the step exists for — the rules are driven against their known-bad fixtures before the lint runs. The pin's expected literal moved with it, and because that pin is payload **equality** it reddened *before* the workflow was touched (`found 0`), which is the ordering evidence that it is a real pin. Drill (s) appended `\|\| true` and reddened it again — the exact mutation a token probe reports as green |
| D-44 | [slice 56 review](reviews/2026-09-04-slice-56-post-slice-review.md) | P3 | `WorkflowShapeTests.testEveryGateInvocationInTheWorkflowIsPinned` tokenizes the WHOLE workflow file, so a `--gate` token inside a YAML comment reddens it. It fails CLOSED, which is the right direction, and the obvious narrowing (scope the census to `run:` payloads) reopens the hole the census exists to close — a thirteenth gate step that no per-step pin can see. Recorded rather than fixed; if it ever fires on a comment, the fix is to reword the comment, not to narrow the census | open |
| D-45 | [slice 57 spec](specs/2026-09-04-wrap-memory-shape-design.md), §2 | P2 | **Two vacuous measurements inside `--memory-shape`, the blocking mode criterion 2 is closed on.** (a) `runMemoryShapeDiagnostics` compared each summary's `coreOwnedBytes` against **the first of its provider group**, which for the `synthetic` group is `x == x`, and for `large_text` matched neither branch and fell through to `comparisonPasses = true` — so the mode's only cross-scenario comparison could not fail, and one scenario was not compared at all. (b) `runVariableMemoryShapeScenario` set `providerLines: bufferedLines`, and `formatMemoryShapeSummary` printed that as `touched_lines`: on the two `variable_uniform` lines the column was the buffered window written twice, not a measurement. Found by reading the mode this slice extends, before extending it — closing a criterion on a comparison that cannot fail is the shape this repository has already shipped five times (`AGENTS.md`, "Never hand-type a budget") | discharged([slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 3): the comparison is now the pure `memoryShapeComparisonFailures([MemoryShapeWindowContribution]) -> [String]` against a **declared** `expectedMemoryShapeWindow` (90), covering all eleven lines — `large_text` and the **first** line included, each with its own recorded red — and reachable under `swift test` (`MemoryShapeComparisonTests`, ten tests). `touched_lines` on the variable half is counted from a `CountingLineMetrics` wrapper, intersected with the buffer range; it still prints 90, because the repair changes the number's provenance and not the number, and the five pre-existing lines stay byte-identical, `checksum=` included. Three of the plan's own drills for this repair could not redden as written and were repaired first — see the record's plan-defects section |
| D-46 | [slice 57 spec](specs/2026-09-04-wrap-memory-shape-design.md), Decision 1 | P3 | **The wrap half of `--memory-shape` observes provider probes, and a probe count sees traversal, not allocation.** A core that allocated without traversing — `Array(repeating:count: totalRows)` inside a cursor — probes nothing and passes every invariant in the mode. No instrument available here closes that: `MemoryLayout<T>.size` counts the inline footprint, so a struct holding an array reports a **pointer** for exactly this case, and RSS lives in the non-blocking `--memory-observation`, where it belongs — `--memory-shape` is a blocking step and a host-dependent RSS assertion inside it would be a gate that fails on the runner rather than on the code. Criterion 2's evidence is therefore scoped to what was measured, and saying "the probe count is the only quantity that would grow" would overstate it. **Named follow-up, so this is a decision and not a shrug:** if the evidence is later judged too indirect, extend the non-blocking `--memory-observation` with one wrap scenario per size at a single width — a deliberate call on evidence, not the accidental extension slice 57's Decision 5 exists to prevent | accepted-risk |
| D-47 | [slice 57](verification/2026-09-04-wrap-memory-shape.md), Task 8 | P3 | **D-15's prescribed dispatcher shape is falsified: under `set -e`, `run_self_test \|\| exit 1` is the UNSAFE spelling and the bare call is the safe one.** Bash's `-e` exemption for a command on the left of `\|\|` applies to the callee's **entire body**, so a returning assertion inside `run_self_test` no longer aborts the shell; execution reaches the closing `echo "self_test=pass"`, that success becomes the function's own status, and the `\|\| exit 1` branch never fires. Measured on `derive-gate-budgets.sh` with an injected returning assertion: bare → `exit 1` printing only the fail line; guarded → `exit 0` printing the fail line **and** a false `self_test=pass`. The five scripts split two ways on `set` — `-euo pipefail` in `derive-gate-budgets.sh`, `harvest-gate-corpus.sh` and `lint-plan-assertions.sh`; `-uo pipefail` with no `-e` in `detect-docs-only-pr.sh` and `cross-target-compile.sh` — and on the two without `-e` the dispatcher shape is irrelevant, because the swallow happens *inside* `run_self_test` either way. That includes `cross-target-compile.sh`, the reference D-15 points at: its guard is inert, and it reads as safe only because nothing in it has ever needed the guard to fire. Latent today — every `assert_*` helper in all five scripts calls `exit 1`, which kills the process regardless of `-e` or `\|\|` context. **The real fix is inside `run_self_test`, not at the dispatcher:** a run-level failure counter that every assertion increments and that `run_self_test` checks before its final `echo`. That is a change to the assertion/harness contract, which is why slice 57 recorded it instead of shipping it | open |
