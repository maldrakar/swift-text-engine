# Debt ledger (cross-arc)

Statuses: `open` / `scheduled(slice-N)` / `discharged(link)` /
`deferred(user, date)` / `accepted-risk`. Escalation: an open P2 whose
origin is ≥ 3 completed slices ago must appear in the next review's
Candidate options (see the `choosing-next-slice` skill). Append rows; never
delete — flip status instead.

| id | born | severity | statement | status |
|---|---|---|---|---|
| D-1 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P2 | Latent asymmetric-SDK-drift misdiagnosis in `cross-target-compile.sh`: `wasm_install_precheck` records failure states only, never success — a bundle that installs but lacks one kind's SDK id re-runs the full bounded-retry ladder against the already-installed bundle and reports `sdk_install_failed` where the truth is `sdk_unresolved_after_install`; fix is a third precheck state (e.g. `bundle_installed_ok`) recorded on successful shared install. Latent under the current 6.2.1 pin, which provides both ids | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): `WASM_BUNDLE_STATE` records `bundle_installed_ok` on successful shared install; `wasm_install_precheck` row 5 returns `sdk_unresolved_after_install`; the short-circuit prints `reason=bundle_installed_id_unresolved`. End-to-end drift scenario asserts exactly one install across both kinds |
| D-2 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P2 | Plan-assertion executability: slice 47's plan carried 16 of 29 assertion sites that could not fail and 4 that could not pass — every gate the plan placed on its own correctness was eyeballed. Encode the review's three conventions (never put a check on the left of a pipe; never `echo "…=$?"` after a command whose exit is insensitive to the invariant; a plan must not assert its own HEAD commit nor both mandate inserting a string and assert zero occurrences of it) into plan-writing practice | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): four plan-assertion conventions in `AGENTS.md`, applied to slice 51's own plan (audited in the review: 0 checks left of a pipe, 13 `${PIPESTATUS[0]}`, no unassigned `$SCRATCH`) |
| D-3 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | Retry logfile overwrite in `cross-target-compile.sh`'s bounded-retry ladder: every attempt writes the same `$logfile`, so `print_log_tail` shows only the last attempt's output when diagnosing an install failure. Debt from the slice 45/46 script work; first review row is slice 47's P3 #6 | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): attempt `i` writes `${logfile}.attempt-${i}` via the pure `attempt_logfile`; the ladder prints one labelled tail per attempt on exhaustion and `prepare_wasm_sdk`'s single `print_log_tail` call is gone |
| D-4 | ruleset config (AGENTS.md bypass caveat) | — | Bypass-capable actors can override the `Main` ruleset; required checks bind normal PR flow only | accepted-risk |
| D-5 | [slice 48 spec](specs/2026-07-21-outer-loop-codification-design.md), Decision 8 | P3 | No mechanical enforcement of the review output contract (e.g. a grep test over `docs/superpowers/reviews/`) — deliberate: watch the ritual run manually for at least one arc first | deferred(user, 2026-07-21) |
| D-6 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | Shell-purity exemption set for `cross-target-compile.sh` is unpinned — it exists in prose only; the `flagName` named-and-justified exemption-set pattern in `WorkflowShapeTests.swift` is the model to copy | discharged([slice 51](reviews/2026-08-08-slice-51-post-slice-review.md), [PR #120](https://github.com/maldrakar/swift-text-engine/pull/120)): `SELF_TEST_COVERED`/`SELF_TEST_EXEMPT` at top level with a justification per exempt entry, enforced by a two-direction partition + a coverage check under four anti-tautology rules; the detector underneath was widened and pinned in the same slice (review P3 #3) |
| D-7 | [slice 46 review](reviews/2026-07-20-slice-46-post-slice-review.md) | P2 | Harvester provenance gap (carried since slice 42): `harvest-gate-corpus.sh` selects rows by run id alone, with no conclusion/event/fork check, so a fork PR's run could in principle print fabricated `p95_ns=` lines that a later harvest ingests — the only unverified link in the calibration chain under twelve blocking budgets | deferred(user, 2026-07-22; re-affirmed 2026-08-06 at the slice-51 selection — different surface, own spec) |
| D-8 | [slice 46 review](reviews/2026-07-20-slice-46-post-slice-review.md) | P2 | Bulk-edit absolute backstop (carried since slice 43): `bulk_structural_mutation` remains exempt from the absolute product ceiling (`BenchmarkMode.isFrameHotPath` excludes it); real recorded gap — needs a product-target decision before any ceiling can be derived | discharged([slice 52](verification/2026-08-08-gate-recalibration-and-bulk-ceiling.md), [PR #123](https://github.com/maldrakar/swift-text-engine/pull/123)): the user's product target (one whole 60 FPS frame for a discrete action) unblocked it; a total two-class `AbsoluteCeiling { scrollFrame, discreteAction }` replaces `isFrameHotPath: Bool`, so **no mode is exempt** — `bulk_structural_mutation` is `.discreteAction` at `GateLimits.frameNanoseconds` = 16_666_666 ns, FIXED, and its gate line now prints a number where it printed `exempt`. `testEveryGatedBudgetIsUnderItsClassCeiling` lost its filter and pins all **46** gated budgets (was 41) against their class ceiling; it is the static enforcement point, the runtime `budget_absolute_exceeded` reason is defense-in-depth |
| D-9 | [slice 46 review](reviews/2026-07-20-slice-46-post-slice-review.md) | P2 | p95 thin axis (carried since slice 41): when the windowed 3x-max floor term relaxes, a p95 budget rests on the 8x-median backup term alone — the thin axis to watch at every re-derivation. **Statement amended in place by [slice 52](verification/2026-08-08-gate-recalibration-and-bulk-ceiling.md), not re-scoped** (the D-15 precedent): the *shape-transition half* is **discharged by fact** — slice 52's harvest flushed the window, which now holds **no** pre-slice-45 run (smallest window id `29701547123`, above the `29692848870` boundary) and **no** run contributing more than one `realistic_provider` row (16 such runs before the harvest → 0 after). That check is **window-scoped**: whole-corpus legacy-shape runs went 33 → 38 in the same harvest and that is correct — the corpus is append-only and the harvester still reads the pre-slice-45 line shape on purpose. The *thin-axis half* stays **open**, and now carries the **rule and the ratio rather than a list**: a p95 budget is median-governed exactly when `max / med <= 2.67` (equivalently `8 x med >= 3 x max`), true for **45 of 46** scenarios after this harvest; the remaining max-governed one is the exposed scenario. `derive-gate-budgets.sh` now prints `gov_p95=median\|max` per scenario, so the observation is re-derivable on demand instead of transcribed — and it must be: the single max-governed scenario moved from `line_query\|uniform_1k` to `column_query\|prefixsum_100k` in this one harvest, so a named list would run to nearly every scenario and be stale on arrival | open |
| D-10 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | `AGENTS.md` still cites `docs/superpowers/verification/2026-06-16-swift-ci-required-checks.md` as a live "see" reference while that doc carries the retired `WASM cross-target observation` context name at 8+ sites with no superseded banner — a reader following the pointer for current policy finds the old name in ruleset JSON | open |
| D-11 | [slice 47 review](reviews/2026-07-20-slice-47-post-slice-review.md) | P3 | The required-check pin models job `name:` only: job-level `strategy:`/`if:` (which change or suppress GitHub's reported context) are unmodeled and the job set itself is unpinned, so a fourth required job would be silently missed — the repo's own "pins must model what runtime reads" lesson, recurring | open |
| D-12 | [slice 49 review](reviews/2026-07-23-slice-49-post-slice-review.md) | P3 | Interior exact-equal wrap-width boundary untested: `greedyEnd`'s inclusive `columnOffset(c) − startOffset <= wrapWidth` is exercised only at the whole-line edge (the equivalence oracle at `width == total`), never at an interior break where a legal end lands exactly on the width. Operator correct by inspection; a `<`-vs-`<=` mutation would redden the whole-line case but not an interior-only fixture. One-line fold-in for node 2 (Slice 50), which touches the same cursor | discharged([slice 50](reviews/2026-07-25-slice-50-post-slice-review.md), [PR #117](https://github.com/maldrakar/swift-text-engine/pull/117)): `testInteriorExactEqualWidthBoundary` added with recorded `<=`→`<` mutation (`endColumn` 2→1) |
| D-13 | [slice 50 review](reviews/2026-07-25-slice-50-post-slice-review.md) | P3 | Per-axis binary-search default triplication: `binarySearchLogicalLine` (`VisualRowLayoutSource.swift`) is the third copy of the same body — after `binarySearchLineIndex` (`LineMetricsSource.swift`) and the column-axis default — differing only in the accessor (`firstVisualRow`/`offset`) and target type (`Int`/`Double`). Plan-governed (spec Decision 1 ratifies a per-axis default); each copy is correct; a bug in the pattern must be fixed in three places. Consolidation into a shared generic helper is a later cross-axis slice (fold-in candidate) | open |
| D-14 | [slice 51 spec](specs/2026-08-07-cross-target-script-hardening-design.md), Decision 8 | P3 | The coverage/exemption classification is pinned for `cross-target-compile.sh` only; `derive-gate-budgets.sh`, `harvest-gate-corpus.sh`, and `detect-docs-only-pr.sh` now have their self-tests enforced by `swift test` but carry no classification, so a new unexercised function in any of them is silent | open |
| D-15 | [slice 51 spec](specs/2026-08-07-cross-target-script-hardening-design.md), whole-branch review | P3 | Dispatcher shape asymmetry across the four `.github/scripts` self-tests: slice 51 changed `cross-target-compile.sh`'s dispatcher from `run_self_test; exit 0` to `run_self_test \|\| exit 1`, leaving `derive-gate-budgets.sh:77` and `harvest-gate-corpus.sh:117` on the old shape. **Statement corrected by the [slice 51 review](reviews/2026-08-08-slice-51-post-slice-review.md) (P3 #1):** the originally-recorded trigger — a `return`-based failure being swallowed by the trailing `exit 0` — *cannot fire*. Both sibling scripts run `set -euo pipefail`, so `-e` aborts the shell before `exit 0` is reached (measured: `run_self_test` returning 1 exits **1** in both, and **0** in `cross-target-compile.sh`, the only one of the four without `-e` — which is exactly why it needed the fix). Remaining substance: a cosmetic consistency nit, not latent risk. Fold-in candidate | open |
| D-16 | [slice 51 spec](specs/2026-08-07-cross-target-script-hardening-design.md), Decision 8 | P3 | The `--self-test` coverage check strips comments and declaration lines from the extracted body but **not string literals**, so a function name appearing inside a quoted assertion label would count as a reference and let an unexercised covered function pass. Deliberate: a shell-grade string parser costs more than the leak is worth, and the partition check still forces every function to be classified either way. No label in the file equals a function name today. Surfaced from the design into the ledger by the [slice 51 review](reviews/2026-08-08-slice-51-post-slice-review.md) so the accepted residual is visible cross-arc. Trigger to revisit: the first assertion label that equals a function name | accepted-risk |
| D-17 | [slice 52 verification](verification/2026-08-08-gate-recalibration-and-bulk-ceiling.md), Task 5 | P2 | **The `${PIPESTATUS[0]}` idiom that `AGENTS.md`'s own D-2 plan-assertion conventions recommend by name (rule 1, `AGENTS.md:639`) does not work in this repo's default shell.** Agent command blocks run under **zsh**, which does not populate `PIPESTATUS` at all (it spells the array `pipestatus`, and indexes from 1), so `${PIPESTATUS[0]}` expands to the **empty string**. It does not merely go quiet: `[ "" -eq 0 ]` evaluates **true** in zsh, so the assertion **inverts into a pass** — precisely the un-failable-assertion defect D-2 exists to prevent, recommended by the rule itself. Observed live in slice 52 Task 5 (`echo "build_exit=${PIPESTATUS[0]}"` printed empty; the exit status had to be re-checked with a plain `$?`). Scope: **no committed script is affected** — all four `.github/scripts/*.sh` are `#!/usr/bin/env bash` and none uses `PIPESTATUS`. The blast radius is agent-run plan/verification command blocks — **including the 13 `${PIPESTATUS[0]}` sites the slice-51 review counted as *evidence of D-2 compliance*** (this ledger's D-2 row; [slice 51 review](reviews/2026-08-08-slice-51-post-slice-review.md) row 7), which are themselves un-failable by this finding, so that compliance evidence does not hold. Remediation to record, **not applied here** (choosing the replacement idiom is its own decision — this branch does edit `AGENTS.md`, in three commits, but rule 1's wording was not one of them): recommend a shell-agnostic form instead — `if ! cmd > /dev/null 2>&1; then … fi`, or `set -o pipefail` inside an explicit `bash -c`, or simply not piping | open |
| D-18 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | The plan's own AC9 checksum assertion fails on a healthy run: Task 8 Step 3 asserts `[ "$N" -eq 46 ]` over `extract_checksums` applied to a hosted run log, but the literal function yields **54** — the non-gate `memory_shape` (5) and `memory_observation` (3) diagnostic steps also print `mode=`/`scenario=`/`checksum=` fields. The *local* §9 extraction never saw it because that capture loops only the twelve gated modes. Fixed in-place in slice 52's verification §12.5 with a `grep -v` filter, but the plan **template** is unfixed and the next slice copying the AC9 recipe will rediscover it. Fold-in for whichever slice next writes an AC9 step | open |
| D-19 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | `frame-hot-path` vocabulary outlives the concept in test prose: five sites in `GateLogicTests.swift`, including the test *name* `testAbsoluteCeilingFiresForFrameHotPathMode` whose sibling reads `…ForBulkModeAtItsOwnCeiling`. Nothing is false — the concept maps 1:1 onto `.scrollFrame` — and slice 52's AC5 scans are identifier-based so they cannot see prose. Renaming restores symmetry. Fold-in for the next slice touching this file | open |
| D-20 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | The four non-gateable modes' `AbsoluteCeiling` class is pinned by nothing: `testDiscreteActionClassIsExactlyDocumented` filters on `isGateable`, so `rangeOnly`, `memoryShape`, `memoryObservation`, and `wrapCompute` classify under a total function but no test covers the choice. Documented at the `absoluteCeiling` switch itself ("do not expect a test to catch a wrong choice here") and inert today, because the absolute tokens are emitted only inside the `includeGate` branch. **The inertness ends the moment a wrap mode becomes gateable** — i.e. at wrap map node 6, where this is the row to read | open |
| D-21 | [slice 52 review](reviews/2026-08-09-slice-52-post-slice-review.md) | P3 | A new **gateable** mode landing in the `.scrollFrame` arm trips no pin. The class-membership pin is bidirectional for `.discreteAction` (moving `bulk_structural_mutation` out, or any other gated mode in, reddens it), but the `.scrollFrame` arm is unpinned. Safe by default — `.scrollFrame` is the *tighter* ceiling and `GateFloorTests` still catches an over-budget mode — but the safety is an accident of which arm is larger, worth naming before wrap modes arrive | open |
